spring:
  flyway:
    enabled: false
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
    open-in-view: false
server:
  port: 8081
# eureka
eureka:
  client:
    enabled: false