spring:
  flyway:
    enabled: true
  rsocket:
    server:
      mapping-path: /rsocket
      transport: websocket
server:
  port: 8081
# eureka
eureka:
  client:
    enabled: false
logging:
  level:
    org.springframework.messaging.rsocket: DEBUG
